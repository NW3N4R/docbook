name: docbook
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.10.4


dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  path: ^1.9.1
  flutter_launcher_icons: ^0.14.4
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  flutter_svg: ^2.2.4
  toastification: ^3.0.3
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
 

dev_dependencies:
  flutter_test:
    sdk: flutter
 

  flutter_lints: ^6.0.0

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/imgs/Icon.png"
  min_sdk_android: 21 # android min sdk min:16, default:21
  adaptive_icon_foreground: "assets/imgs/Icon.png"


# The following section is specific to Flutter packages.
flutter:
  generate: true
  uses-material-design: true

  assets:
     - assets/
     - assets/fonts/
     - assets/imgs/

  # fonts:
  #  - family: Rabar_019
  #    fonts:
  #      - asset: assets/fonts/rabar19.ttf 

  #  - family: Rabar_032
  #    fonts:
  #     - asset: assets/fonts/rabar32.ttf

  #  - family: Rabar_033
  #    fonts:
  #      - asset: assets/fonts/rabar33.ttf
       
  #  - family: RudawRegular
  #    fonts:
  #     - asset: assets/fonts/rudawregular2.ttf